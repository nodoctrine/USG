Everywhere:
- DONE Replace all references to mb or mybooks. Change to -usg or USG. Be very consistent.
- DONE Modify "Challenge" (or related abbreviations) to "Activity" in all locations
- do a pass to make everything work on mobile
	- one example is that saving progress is unreliable on mobile

shared-nav.js
- DONE Remove "Tour" function from around Lines 102-256 in shared-nav.js
- what isRemove backwards compatibility (and modify all previous chapters to use the current functions)
	- Remove duplicate functions (markDone,renderProgress, etc.) from older chapter inline scripts — they are already handled by shared-nav.js and the copies are dead code.

 